{
  "name": "delivery-platform-functions",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "deploy": "firebase deploy --only functions",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "engines": {
    "node": "24"
  },
  "main": "lib/index.js",
  "dependencies": {
    "axios": "^1.6.0",
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^7.2.2",
    "stripe": "^14.5.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^5.2.2"
  }
}
